# Joomla to WordPress converter
## Usage
 * Open `config.php`
 * Edit the path of `wp-load.php` correctly
 * Configure the database parameters of your Joomla database
 * Navigate to `index.php` and run links serially